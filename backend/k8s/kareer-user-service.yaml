apiVersion: v1
kind: Service
metadata:
  name: be-user
  namespace: backend
spec:
  selector:
    app: be-user
  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP
